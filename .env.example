PORT=3000
NODE_ENV=development

# Supabase Postgres (pooler). Encode special chars in password (e.g. @ → %40)
# Tables live in the "sewadal" schema (does not touch other public tables).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"

UPLOAD_PATH=./uploads
CORS_ORIGIN=http://localhost:5173

# Login credentials
AUTH_USERNAME=Sewadal2026
AUTH_PASSWORD=
AUTH_SECRET=
SESSION_SECRET=

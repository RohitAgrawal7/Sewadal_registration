[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Optional override; schema already defaults to prisma/dev.db
  DATABASE_URL = "file:./dev.db"
  PRISMA_CLI_BINARY_TARGETS = "native,debian-openssl-3.0.x"
  SESSION_SECRET = "swaadal-session-change-me-in-netlify"

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Set DATABASE_URL, SESSION_SECRET / AUTH_SECRET, AUTH_USERNAME, AUTH_PASSWORD
  # in the Netlify / Vercel dashboard (Supabase Postgres). Do not hardcode secrets here.
  PRISMA_CLI_BINARY_TARGETS = "native,debian-openssl-3.0.x,rhel-openssl-3.0.x"
